@model dynamic

<div class="col-md-3">
    <h4>@Model.Title</h4>
    @foreach (var story in Model.Items)
    {
        <partial name="_StoryCard" model="story" view-data='@new ViewDataDictionary(ViewData) { { "Color", Model.Color } }' />
    }
</div>